---
title: "EDA"
format: pdf
editor: visual
author: Cooper Ruffing
---

```{r}
#| label: "load data and libraries"

library(readxl)
library(tidyverse)
library(dplyr)
library(broom)
library(MASS)
library(AER)
library(car)
library(sandwich)
library(lmtest)

totaldata <- read_excel("aggregated_dataset.xlsx")

```

```{r}

ggplot(totaldata, aes(x = period_start, y = `Total Attacks in Segment`)) +
  geom_point()

ggplot(totaldata, aes(x = `Total Attacks in Segment`, y = avg_distance_km)) +
  geom_point()

ggplot(totaldata, aes(x = `Catchment Area (km²)`, y = `Percentage of Total Attacks`)) +
  geom_point()

ggplot(totaldata, aes(x = avg_distance_km, y = `Catchment Area (km²)`)) +
  geom_point()

```

```{r}
#| label: injury totals and proportions

totaldata <- totaldata |>
  mutate(totinjuries = Burn + CMF + Limb + NTD + `Soft Tissue` + `Wound Care`) |>
  mutate(burnprop = Burn / totinjuries) |>
  mutate(cmfprop = CMF / totinjuries) |>
  mutate(limbprop = Limb / totinjuries) |>
  mutate(ntdprop = NTD / totinjuries) |>
  mutate(stprop = `Soft Tissue` / totinjuries) |>
  mutate(wcprop = `Wound Care` / totinjuries)

totaldata <- totaldata |>
  mutate(across(where(is.numeric), ~ replace(., is.nan(.), 0)))

```
